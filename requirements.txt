# web_demo/requirements.txt — Streamlit Community Cloud / local (no Docker)
# CPU-only PyTorch keeps install size and RAM lower than the default CUDA build.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.13.0

streamlit==1.61.1
pdfplumber==0.11.10
sentence-transformers==5.7.0
faiss-cpu==1.15.0
numpy==2.4.6
openai==3.1.0
python-dotenv==1.1.0
